import { OSCalendar, OSGantt } from '../'

describe('OSCalendar', () => {
  it('is defined', () => {
    expect(OSCalendar).toBeDefined()
  })
})

describe('OSGantt', () => {
  it('is defined', () => {
    expect(OSGantt).toBeDefined()
  })
})
